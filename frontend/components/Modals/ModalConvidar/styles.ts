import { StyleSheet } from "react-native"

export const styles = StyleSheet.create({
  
  modal_codigo_container: {
    gap: 0,
    marginBottom: 24,
  },

  modal_codigo_texto: {
    fontFamily: "HeptaSlab-SemiBold",
    textAlign: "center",
    fontSize: 32,
    letterSpacing: 8,
  },
})
